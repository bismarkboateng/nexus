import PropertyWrapper from "@/app/(property)/components/property-wrapper";

export default function Page() {
  return (
    <section className="w-full h-full grid grid-cols-5 gap-14 mt-1">
      <PropertyWrapper />
    </section>
  );
}
